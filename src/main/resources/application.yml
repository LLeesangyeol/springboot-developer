spring:
  datasource: #메모리 임시 DB 생성
    url : jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true # 웹 콘솔 활성화
      path : /h2-console

  jpa:
    # 전송 쿼리를 확인
    show-sql: true
    properties:
      hibernate:
        format_sql:true

    defer-datasource-initialization: true


